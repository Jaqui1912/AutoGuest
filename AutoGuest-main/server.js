/**
 * ENTRY POINT LEGACY — No usar directamente.
 * El proyecto fue reestructurado. El entry point correcto es src/server.js
 * Ejecutar: npm start  (usa `node src/server.js` según package.json)
 *
 * Este archivo delega al nuevo entry point para compatibilidad.
 */
require('./src/server');